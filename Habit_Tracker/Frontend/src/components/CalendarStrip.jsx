// Kept so older imports keep working. The seven-day strip is now HabitChain,
// which draws consecutive days as connected links instead of isolated ticks.
export { default } from "./HabitChain.jsx";
